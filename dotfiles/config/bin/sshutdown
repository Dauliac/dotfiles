#!/bin/sh
# This script is a verification to don't lost data with failed reboot
pgrep -x dmenu && exit

choice="$(printf "No\\nYes" | dmenu -i -p "Do you want to stop me cocksucker ?" | awk '{print $1}')"
[ -z "$choice" ] && exit 1

[ $choice -eq "Yes"] && systemctl shutdown
